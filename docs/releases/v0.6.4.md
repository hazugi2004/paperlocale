# PaperLocale 0.6.4

本版基于一次 13 页渔业气候论文翻译的真实故障改进控制与局部修复：

- 首错停止：`--no-contract-repair` 保存成功片段与失败证据后，阻止第二次模型调用；模型与推理强度不变。
- 中文修复：按真实字宽换行、写入前内存预检；显式 `--min-font-size` 可限制缩小范围，失败不改变候选或验收状态。
- 片段安全：兼容丢失空格的标题片段定位，仍使用源 PDF 的真实单词边界，不猜测完整标题、不自动翻译残片。

## 验证边界

真实任务使用 0.6.3、gpt-6-astra、medium；128 个片段完成。作者姓名透传确认、源矢量恢复及第 1/2/5/8 页局部修复后，13 页结构 QA 无错误/警告，逐页视觉验收完成。最终 PDF SHA-256：`6d10edbc930229db648339ff86cb7730be80a3206ce5dc8d63a942ea175db140`。

本版不宣称自动解决所有版面问题。作者例外仍须源文证据；矢量恢复仍绑定源/译文哈希；通过机器 QA 后仍需全页视觉复核。未公开论文 PDF 或完整译文，回归测试使用自有合成数据。

0.6.4 本地通过 179 项测试；另在真实任务备份上验证第 2 页中文自动换行，保持 8.2pt 字号、区域外内容不变，并检出缺失空格的拆分标题。该验证副本没有替换已验收交付文件。
