# PaperLocale 0.6.3

修复 GPT 在第 30 篇实际翻译中触发的两处内容门禁误判。

- 门禁在临时校验副本中统一英文面积/体积名称及明确的千、百万、十亿倍率，正确接受 `2-5.7 million square kilometers` 对应 `2-5.7百万平方千米` 或 `2-5.7百万km²`。不改写译文，不换算单位；缺少倍率、改变维度或数值仍拒绝。
- 纯网址片段可原样保留；增加无协议 `www.` 网址的一致性检查。混合片段中的真实长正文仍须翻译，URL、DOI、数字和公式门禁继续生效。
- Qwen 请求构造、分段、保护标记、单位识别与重试实现保持不变；GPT 请求适配器也不变。修复只作用于两条路径共用的结果校验。

验证：173 项本地测试通过，包含 GPT/Qwen 既有回归、上述实际失败候选及倍率/维度/网址损坏反例。通过测试不代表可保证所有模型输出或任意 PDF 排版成功；真实信息遗漏仍会暂停。
